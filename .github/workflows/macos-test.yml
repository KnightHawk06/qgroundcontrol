name: macOS Test Build

on:
  push:
    branches:
      - issue-7133-macos-test
  pull_request:

env:
  QT_VERSION: "6.5.3"

jobs:
  build-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python (avoid PEP 668)
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Toolchain info
        run: |
          which python3
          python3 --version
          which cmake || true
          cmake --version || true
          uname -a

      - name: Cache Qt
        uses: actions/cache@v4
        with:
          path: ~/Qt
          key: qt-${{ runner.os }}-${{ env.QT_VERSION }}

      - name: Install aqtinstall
        run: |
          python3 -m pip install --upgrade pip
          python3 -m pip install aqtinstall

      # Optional: list available modules for visibility
      - name: List available Qt modules
        run: |
          python3 -m aqt list-qt mac desktop "${QT_VERSION}" --modules macos

      - name: Install Qt (aqt, no Homebrew)
        run: |
          set -eux
          if [ ! -d "$HOME/Qt/${QT_VERSION}/macos" ]; then
            # Note: include the arch 'macos' before options; modules are space-separated
            python3 -m aqt install-qt mac desktop "${QT_VERSION}" macos -O "$HOME/Qt" \
              -m qtdeclarative qtquickcontrols2 qtpositioning qtserialport qtmultimedia qtsvg qtlocation qtshadertools
          fi
          ls -la "$HOME/Qt/${QT_VERSION}/macos"

      - name: Ensure Ninja
        run: |
          ninja --version || brew install ninja

      - name: Configure (CMake + Qt6)
        env:
          CMAKE_PREFIX_PATH: ${{ runner.home }}/Qt/${{ env.QT_VERSION }}/macos
        run: |
          set -eux
          mkdir -p build
          cd build
          cmake .. \
            -G Ninja \
            -DCMAKE_BUILD_TYPE=Release

      - name: Build
        run: |
          set -eux
          cd build
          ninja -v
